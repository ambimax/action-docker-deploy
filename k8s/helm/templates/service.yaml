apiVersion: v1
kind: Service
metadata:
  name: "{{ include "chart.fullname" . }}"
  labels:
{{ include "chart.labels" . | indent 4 }}
spec:
  type: NodePort
  selector:
    app: "{{ .Values.app }}"
  ports:
  - port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
